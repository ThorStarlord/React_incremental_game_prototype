import React from 'react';

const Inventory = () => {
    return (
        <div className="inventory">
            <h2>Inventory</h2>
            {/* Display inventory items here */}
        </div>
    );
}

export default Inventory;