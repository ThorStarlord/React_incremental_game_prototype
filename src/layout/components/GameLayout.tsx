/**
 * @file GameLayout.tsx
 * @description Re-exports the main GameContainer component.
 * This file might be simplified or removed depending on routing setup.
 */
import GameContainer from './GameContainer'; // Import the refactored container

// Re-export GameContainer as GameLayout if needed for compatibility
const GameLayout = GameContainer;

export default GameLayout;
