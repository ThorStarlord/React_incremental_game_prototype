/**
 * Current product version. This is intentionally a beta version until the
 * external 1.0 release gates are closed and an immutable candidate is tagged.
 */
export const APP_VERSION = '0.9.0-beta.1';
