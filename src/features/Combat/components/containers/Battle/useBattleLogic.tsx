/**
 * Re-export the modularized useBattleLogic hook
 * This maintains backward compatibility with the original file location
 */
import useBattleLogic from '../../../hooks/battle/useBattleLogic';
export default useBattleLogic;
