/**
 * Copy Feature Public API
 * Re-export state, types, thunks, and selectors for convenient imports.
 */
export * from './state';

// Optionally expose key UI components here if needed by pages
// export { default as CopyTraitSlots } from './components/ui/CopyTraitSlots';
